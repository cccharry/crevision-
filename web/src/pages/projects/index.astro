---
// 作品集页：与本地 projects.html / projects-page.html 视觉一致（静态），不经 KV 列表占位
import raw from '../../data/projects-page.html?raw';
const page = raw.replace(/^\s*<!DOCTYPE html>/i, '').trimStart();
---
<Fragment set:html={page} />

<script>
  import { bootPublicProjectsPage } from '../../scripts/public-cms-projects-page.ts';
  void bootPublicProjectsPage();
</script>
