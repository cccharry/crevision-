---
// 品牌官网首页：与仓库内 index_副本.html / brand-home.html 内容一致，无 CMS 或作品列表接口
import raw from '../data/brand-home.html?raw';
// Astro 会为页面自动输出一层 DOCTYPE，去掉源文件里的首行，避免重复
const page = raw.replace(/^\s*<!DOCTYPE html>/i, '').trimStart();
---
<Fragment set:html={page} />
