# Pages 构建：根目录执行 cd web && npm ci && npm run build，输出 web/dist
# 密码 ADMIN_PASSWORD / SESSION_SECRET 仅在 Cloudflare 控制台配置，勿写在此文件
name = "crevision"
pages_build_output_dir = "web/dist"
compatibility_date = "2025-01-01"

[[kv_namespaces]]
binding = "CMS_KV"
id = "7c9b2bd5b7ee45a584c492f616a81ce8"
